#include "main.h"
/**
 * mul - cheack if the char upper case or lower
 *
 * @a: the ASCII value of the char
 *
 * @b: the sec num
 * Return: return one if true and zero if false
*/
int mul(int a, int b)
{
	return (a * b);
}
